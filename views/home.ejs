<title>HomePage</title>
<%- include('./partials/nav') %>
    <link rel="stylesheet" href="/stylesheets/home.css">
    <section>
        <% if (user) { %>
            <h1>Welcome to Recipely <%= user.username %> 🧑‍🍳</h1>
            <form action="/auth/sign-out?_method=DELETE" method="POST" class="sign-out">
                <button type="submit">Sign Out</button>
            </form>
            <% } else { %>
                <form>
                    <h2>Do you have account? </h2>
                    <div class="container-1">
                        <div class="response">
                            <a href="/auth/sign-in">Sign in</a>
                            <br>
                            <br>
                            <a href="/auth/sign-up">Create account</a>
                        </div>
                        </button>
                    </div>
                </form>
                <% } %>
    </section>

    </body>

    </html>